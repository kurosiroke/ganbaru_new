<p id="notice"></p>
<div class="container col-8">
    <h3>取り組みリスト確認</h3>
        <table class="table">
           <tbody>
            <tr>
                <td><%= @action.created_at.strftime('%Y/%m/%d') %></td>
                <td>
                    <%= @action.content %>
                </td>
                <td><%= link_to "編集", edit_gaction_path, class: 'btn btn-sm btn-success' %></td>
                <td><%= link_to "削除", gaction_path, method: :delete,  "data-confirm" => "本当に削除しますか？" ,class: 'btn btn-sm btn-danger' %></td>
            </tr>
            </tbody>
        </table>
</div>