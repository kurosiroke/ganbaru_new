<p id="notice"></p>
<div class="container col-8">
    <h3>取り組みリスト確認</h3>
            <table class="table">
               <tbody>
                   <%# @action.each do |action| %>
                <tr>
                    <td><%= @action.created_at.strftime('%Y/%m/%d') %></td>
                    <td>
                        <%= @action.content %>
                    </td>
                    <td></td>
                    <td></td>
                </tr>
                    <%# end %>
                </tbody>
            </table>
</div>