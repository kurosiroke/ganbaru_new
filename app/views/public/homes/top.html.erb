<p id="notice"></p>
<div class="container col-8">
    <div class="text-center">
        <h2>一人じゃない！　みんながんばってる！</h2>
    </div>
        <div>新着&emsp;みんなのがんばる！</div>
        <table class="table table-hover table-inveres">
            <thead>
                <tr>
                    <th>日付</th>
                    <th>がんばるリスト</th>
                    <th>投稿者</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            <% @ganbaru.each do |action| %>
            <tr>
                <td><%= action.created_at.strftime('%Y/%m/%d') %></td>
                <td>
                    <%= link_to action_path(action.id) do %>
                    <%= action.content %>
                    <% end %>
                </td>
                <td>
                    <%= link_to @user do %>
                    <%= action.user.name %></td>
                    <% end %>
                </td>
            </tr>
            <% end %>
            </tbody>
        </tabel>
</div>

