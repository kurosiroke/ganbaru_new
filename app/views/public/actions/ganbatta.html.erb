<div class="container col-10">
    <div class="row">
        <!--<div class="col-md-3">-->
        <%#= render 'public/users/list', user: current_user, action: @action %><%# userでcuent_userを渡した%>
        <!--</div>-->
        <div class="col-md-8 offset-md-1">
        <h2>がんばった</h2>
            <table class="table table-hover table-inveres">
            <thead>
                <tr>
                    <th>日付</th>
                    <th>がんばった！　リスト</th>
                    <th></th>
                </tr>
            </thead>
        <tbody>
       <% @ganbatta.each do |action| %>
          <tr>
            <td>
                <%= link_to user_path(book.user) do %>
                <%= %>
                <% end %>
            </td>
            <td><%= action.content %></td>
          </tr>
       <% end %>
        </tbody>
      </table>
     </div>
  </div>
</div>