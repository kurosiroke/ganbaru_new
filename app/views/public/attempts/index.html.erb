<div class="container col-10">
    <div class="row">
        <div class="col-md-3">
        <%= render 'public/users/list', user: @user, action: @action %>
        </div>
        <div class="col-md-8 offset-md-1">
        <h2>がんばる</h2>
            <table class="table table-hover table-inveres">
            <thead>
                <tr>
                    <th>日付</th>
                    <th>がんばる！　リスト</th>
                    <th></th>
                </tr>
            </thead>
        <tbody>
      <% @ganbaru.each do |action| %>
          <tr>
            <td>
                <%= link_to user_path(current_user) do %>
                <%= %>
                <% end %>
            </td>
            <td><%= action.content %></td>
          </tr>
       <% end %>
       <% @ganbatta.each do |action| %>
          <tr>
            <td>
                <%= link_to user_path(current_user) do %>
                <%= %>
                <% end %>
            </td>
            <td><%= action.content %></td>
          </tr>
       <% end %>
        </tbody>
      </table>
     </div>
  </div>
</div>