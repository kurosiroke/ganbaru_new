     <table class="table table-hover table-inveres">
        <thead>
          <tr>
            <th>がんばったこと一覧</th>
          </tr>
        </thead>
        <tbody>
        <% @ganbattas.each do |attempt| %>
          <tr>
            <td><%= attempt.created_at.strftime('%Y/%m/%d') %></td>
            <td>
              <%= link_to attempt_path(attempt.id) do %>
              <%= attempt.content %>
              <% end %>
            </td>
            <td>
              <%= attempt.user.name %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>