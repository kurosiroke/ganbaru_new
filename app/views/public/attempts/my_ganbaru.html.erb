<div class="container col-10">
  <div class="row mt-3">
    <div class="col-2">
    <%= render 'public/mypages/list', user: current_user, attempt: @attempt %>
    </div>
    <div class="container col-9">
      <div class="row">
        <div class="col-md-8 offset-md-1">
        <h2>自分のがんばる！</h2>
          <table class="table table-hover table-inveres">
            <thead>
              <tr>
                <th>日付</th>
                <th>がんばるリスト</th>
                <th>タグ</th>
                <th>投稿者</th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <%= render 'public/attempts/my', user: current_user, attempt: @attempt %>
          </table> 
          <%= paginate @attempt %>
        </div>
      </div>
    </div>
  </div>
</div> 